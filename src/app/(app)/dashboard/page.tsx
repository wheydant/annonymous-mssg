import React from 'react'

//Creating an (app) it wont be poping in url but can store layout and page in it so that we can use it for dashboards cards
const page = () => {
  return (
    <div>Dashboard</div>
  )
}

export default page